apiVersion: apps/v1
kind: Deployment
metadata:
  name: jassa-bot
  namespace: default
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      workload.user.cattle.io/workloadselector: deployment-default-jassa-bot
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: jassa-bot
      annotations:
        deploy-date: "deploy-date-value"